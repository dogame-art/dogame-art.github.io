name: 'Automatic build'
on:
  push:
    branches:
      - main
    paths-ignore:
      - .gitignore
      - README.md
      - LICENSE
  workflow_dispatch:
  schedule:
    - cron: '0 */3 * * *'  # Runs every 3 hours (UTC) - fixed the syntax
jobs:
  continuous-delivery:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
          
      # NEW: Debug - Check what files are actually there
      - name: Debug - List files in repository
        run: |
          echo "Current directory:"
          pwd
          echo "Files in current directory:"
          ls -la
          echo "Looking for package.json specifically:"
          find . -name "package.json" -type f
          
      # NEW: Setup Node.js for artwork generation
      - name: Setup Node.js for artwork generation
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          
      # NEW: Generate artwork pages before main build
      - name: Generate artwork pages
        run: |
          echo "Installing npm dependencies..."
          npm install
          echo "Generating artwork pages..."
          npm run build
          echo "Artwork generation complete!"
          echo "Files created:"
          find . -name "WindowShopping" -type d
          find . -name "gallery" -type d
          echo "All directories in root:"
          ls -la
          
      # Your existing Ruby setup
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 3.4.2
          bundler-cache: true
          
      # Your existing deploy step
      - name: Deploy
        run: bash deploy.sh
